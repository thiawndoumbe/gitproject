const name = "Ilya";
    alert(`hello ${1+2}`);//hello 3 les `` permet de creer un code java scrip a l'interieur cest une facon de concatener
    alert(`hello ${"name  myname"}`); // hello name
    alert(`hello ${name}`); // hello Ilya 
    // prompt = donner la main a l"utilisateur c comme le input pour poser plusieurs questions on met plusieurs pompt
    let age =prompt("quel est votre age")
    // prompt donne une chaine de caractere
    alert(age);
    // confirm permet de poser une question a l'utilisateur

    // a==b on compare les valeur alors que  === on compare la valeur et la donnee le type