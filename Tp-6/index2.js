// ++a preincrimentation operation d'incrementation avant de l'affecter
//b++ postincrementation l'affectation d'aboort avant de faire le calcul d'incrementation
// autre facon de convertir une chaine de caractere en nombre on met + devant la chaine
// let a =Number( prompt("First number",1));
// let b= Number( prompt("Second number",2));
// alert(a+b);

// let a =+( prompt("First number",1));
// let b= +( prompt("Second number",2));
// alert(a+b);

let browser="Chrome"
    if (browser=="Edge"){
        alert("You've got the Edge")
    }
    else if (browser=="Chrome" || browser=="Firefox" || browser=="Safari" || browser=="Opera"){
        alert("okay we support theses browser too ")
    }
    else {
        alert("we hepe that this page looks ok ")
    }
    
